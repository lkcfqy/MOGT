# Archived Benchmark Runs

This directory stores benchmark artifacts that are useful for diagnostics but
should not be included in the current main evidence index.

Current archived item:

- `synthetic_last_value_mogt_identity_coupled_value_forget_dense_stdreport_ctx512_seed7_steps2000.json`:
  accidental train-512 MOGT rerun without the intended `value_gate_bias=-2.0`
  setting. It is kept as a sensitivity diagnostic rather than a main-table run.
