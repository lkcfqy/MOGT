# Paper Draft

Build from this directory with:

```bash
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

This is a research draft, not a submission-ready top-tier paper. The current
evidence supports a scoped mechanism paper about coupled write-forget affine
transport and prefix-conditioned slot addressing for recurrent state tracking,
not a claim that MOGT replaces Transformers.

Current paper-ready result snapshot: `paper/results_snapshot.md`, generated by
`summarize_paper_results.py`.

Fast-close status and VM handoff notes are in
`docs/PROJECT_FREEZE_20260505.md`.
